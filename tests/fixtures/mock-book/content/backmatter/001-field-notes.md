# Field Notes

- The manuscript source is Markdown plus YAML.
- The backend renders workspace files when the reader APIs are requested.
- The frontend reads the signed token and fetches rendered content through the reader APIs.
