# Preface

This mock manuscript exists so the local reader can be exercised before any real conversation-owned book has been created.

It is intentionally short, but it uses the same workspace format that the backend renders for normal books.
